/// Barrel export for the profile "Settings" section widget.
///
/// This file used to contain the full implementation directly (659 lines).
/// It has been split into `settings_section/` by responsibility — see that
/// folder for the actual source. This barrel keeps the original import
/// path
/// (`package:app/features/profile/presentation/widgets/settings_section.dart`)
/// working unchanged for existing consumers.
library;

export 'settings_section/settings_section.dart' show SettingsSection;
