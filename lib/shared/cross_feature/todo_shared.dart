/// Cross-feature facade for `features/todo`.
///
/// `home`'s dashboard shows a preview tile of the student's upcoming
/// to-dos, reusing `todo`'s own tile widget instead of duplicating it.
/// See `auth_shared.dart` for the full rationale.
library;

export '../../features/todo/presentation/widgets/variants/todo_preview_tile.dart';
