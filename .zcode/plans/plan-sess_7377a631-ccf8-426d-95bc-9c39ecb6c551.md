# خطة الإصلاح النهائية — EduZone Student App

**المصدر المرجعي للـ schema:** `D:\projects\EduZone\web_project\New_edu_dashboard\supabase\schema` — شجرة عمله مطابقة بايت-بايت لمرآة التطبيق (تحققت). الـ RPC موجود في شجرتي العمل فقط، غير موجود في HEAD التطبيق (`40c99f4`).
**حقائق مثبتة من مراجعتك وفحصي:** تسجيل الجلسات الحالي معطّل بالكامل (region_id='' → FK violation → ابتلاع صامت) · قنبلة الاختبارات منفجرة فعلاً · الـ RPC يقرأ IP من `request.headers` (النهج الصحيح تجاه الـ pooler، لا `inet_client_addr`).

**قاعدة كل مرحلة:** بوابة `flutter analyze` + `flutter test` خضراء + commit تقليدي. لا تحديث dependencies.

---

## المرحلة 0 — تثبيت خط الأساس (commits منفصلة)
1. **Dart المتماسك:** `fix(auth): record session only on fresh login, decoupled from device binding` (auth_provider + auth_activity_sync_service + توثيق قرار التسجيل في courses_provider/enroll_action_button + اختباراته).
2. **مرآة الـ schema + الخادم:** `chore(schema): sync mirror with New_edu_dashboard` (8 ملفات schema + `functions/send-push-notification` بـ timeouts الـ FCM وbatch_size). ملفا `deploy.sh`/`apply_extend_enrollment.js` فرق أسطر فقط (LF/CRLF) — تُركان كما هما. `supabase/fix_mv_populate.js` (غير متتبع): سؤال سريع لك وقت التنفيذ — commit كأداة صيانة أم نقله خارج المرآة.
3. **بوابة نشر تخصك (أول خطوة تنفيذية):** التحقق أن ملفات الـ schema **ملتزمة في مستودع dashboard** (أجرب أوامر git عليه فور البدء؛ إن منعتها البيئة — sandbox يمنع git على مسارات خارج المشروع — تؤكد أنت أن الـ RPC ملتزم ومنشور على قاعدة التطوير عبر `supabase/deploy.js`). **ترتيب النشر: schema أولاً ثم التطبيق.**

## المرحلة 1 — إصلاح قنبلة الاختبارات (منفجرة — P0)
`test/core/feature_flags/feature_flag_cache_test.dart:11`:
```dart
final evaluatedAt = DateTime.fromMillisecondsSinceEpoch(
    DateTime.now().millisecondsSinceEpoch, isUtc: true);
```
(قصّ المللي-ثانية لضمان نجاح `isAtSameMomentAs` بعد تخزين ms). اختبارات staleness تعمل كما هي. المعيار: **المجموعة كاملة خضراء**. Commit: `test(core): fix time-bomb in feature flag cache tests`.

## المرحلة 2 — ترحيل تسجيل الجلسات إلى الـ RPC (P0 — إصلاح كتابة معطوبة)
**المعيار النهائي: صف يظهر فعلاً في `sessions` بعد دخول تجريبي** (تتحقق أنت على بيئة التطوير — غير قابل للإثبات من unit tests).
1. `auth_remote_ds.recordSession()` → `rpc('record_current_session', {'p_device_fingerprint': ..., 'p_user_agent': ...})` عبر `NetworkGuard.write` + `_mapRpcException` (نمط `bindDevice`)؛ حذف استعلام devices + insert الـ sessions؛ تضييق التوقيع إلى `{deviceFingerprint, userAgent}` (الباقي يشتقه الخادم من auth.uid()).
2. `auth_activity_sync_service.dart`: تحديث الاستدعاء (إسقاط regionId)، إبقاء بناء userAgent.
3. **تنظيف الحقل الميت:** `AppUser.regionId` (دائماً '') يصبح بلا استخدامات — إزالته من الكيان/copyWith أو توثيقه إن كسر شيئاً غير متوقع (أقرر حسب الـ diff).
4. تحديث `auth_activity_sync_service_test.dart` + اختبار datasource جديد لشكل استدعاء الـ RPC. Commit: `feat(auth): record sessions via record_current_session RPC`.

## المرحلة 3 — اصطياد إبطال الجلسة من كل RPC (P1، ثغرة 28000)
نمط `SecurityService.killAppHandler` القائم:
1. `core/error/exceptions.dart`: `SessionRevokedException`.
2. `network_exception_mapper.dart` فرع PostgrestException: `code=='28000'` أو رسالة `Invalid user session`/`AUTH_REQUIRED` → خطاف `SessionRevocationHook.notify()` (ملف جديد core/network، static callback خامل) + إرجاع الاستثناء المخصص.
3. `auth_provider` يوصّل الخطاف عند البناء → مسار `handleAccessDenied` (خروج + wipe + unauthenticated)، مقيّداً بحالة authenticated فقط.
4. اختبارات mapper + الخطاف. Commit: `fix(core): detect session revocation (28000/AUTH_REQUIRED) from any RPC`.

## المرحلة 4 — إخراج Supabase من الطبقات الممنوعة (P1)
- **4a (widgets + providers):** استبدال `SupabaseService.client.auth.currentUser?.id` بـ `currentUserIdProvider` (يُنشأ في auth providers): `add_todo_bottom_sheet:57`, `youtube_player_wrapper:134`, `player4_wrapper:414`, `modern_player_wrapper:164`, **`notifications_provider:35,61`**.
- **4b:** نقل `CleanupScheduler` → `features/downloads/data/services/` + تحديث المستدعين. **يحل مخالفة الطبقة فقط** — قيد WorkManager isolate بلا DI يبقى ديناً موثقاً.
- **4c:** نقل rpc من خدمات application إلى datasources: `check_student_app_access_service` → `auth_remote_ds.checkStudentAppAccess()`، `logout_orchestrator` → methods في datasource، `offline_policy_engine` → downloads datasource. `push_token_registration_service` (shared) → `core/services/`.
- **4d:** نقل إنشاء datasources من providers (`todo_provider:25`, `downloads_provider:51`) إلى DI؛ `supabaseClientProvider` → `core/network/supabase_client.dart`.
- **4e:** `feature_flag_repository` → `core/feature_flags/data/feature_flag_remote_ds.dart` (نمط core/logging/data القائم)؛ توحيد وصول العميل في `security_service`/`location_service` عبر core/network.
Commits منفصلة: `refactor(core)/refactor(downloads)/refactor(auth)/refactor(shared)`.

## المرحلة 5 — الامتثال الكامل: إذابة الـ barrels (P1، موجة ببوابة بعد كل خطوة)
- **5a نقل الكيانات المشتركة إلى `shared/models/`:** auth: `account_status`, `user_role`, `auth_state`, `update_info` · downloads: `download_enums`, `downloaded_lesson` · courses: `course`, `lesson`, `section` · todo: `todo_item`. حذف المولدات القديمة + build_runner + تحديث الاستيرادات.
- **5b نقل 4 widgets إلى `shared/components/`** بعد التأكد أنها لا تستورد دواخل ميزاتها: `quality_selector`, `optional_update_dialog`, `todo_preview_tile`, `notification_tile`.
- **5c حل الـ barrels:** الـ 23 موقع استيراد — auth_shared → `features/auth/providers` مباشرة (مسموح)، البقية → shared/models أو shared/components؛ ثم حذف `shared/cross_feature/`.
- **5d حواف provider المتبقية** (video→courses/home): شاشات video_player تستقبل بيانات الدرس/الاستكمال عبر معاملات بناء يمررها `app_router.dart` (طبقة app تحرية)، والـ notifiers بمعاملات methods.
Commits: `refactor(shared)/refactor(courses)/refactor(video_player)`.

## المرحلة 6 — الواجهة (P2)
1. 5 مواضع `ElevatedButton` + `settings_section:354` → `AppButton` (إضافة variant `dangerOutline` إن لم يوجد).
2. 9 فروع error (home:281,374, course_details:73, course_preview:155, discover, notifications, video_player:279,291, offline_player) → `ErrorState` مع retry.
3. 32 `SizedBox` رقمي → أقرب AppSpacing · روابط settings_section → AppConstants · توحيد Icons.* مع AppIcons. Commit لكل حزمة + فحص بصري.

## المرحلة 7 — AdaptiveLayout (P2)
1. إنشاء `lib/core/layout/adaptive_layout.dart` (غلاف فوق Breakpoints) · 2. تصحيح AGENTS.md للقيم الفعلية 600/900/1200 · 3. تطبيق جزئي: MainShell + home + discover + courses + downloads. Commits: `feat(core)` + `docs`.

## المرحلة 8 — الاختبارات الناقصة (P2)
`notifications_provider_test`, `profile_provider_test`, `download_network_policy_provider_test` (mocktail بلا شبكة). Commit: `test(...)`.

## المرحلة 9 — تحسينات دقيقة (P3)
1. مسار login عند maintenance/app_locked: مواءمة المصفوفة (شاشة الحالة دون تدمير الجلسة).
2. Realtime: إضافة suspended للفوري + `verifyAccess()` دورية لـ locked/banned.
3. `download_manager.dart` — **إعادة فتح الملف وقت التعديل** (أسطر catch الفعلية ~72/137): حذف تفاصيل الاستثناء/stack ذات الروابط الموقعة.
4. AGENTS.md: إزالة إشارة cross_feature، توثيق نمط core/*/data، ملاحظة `flutter clean` قبل release build بعد جلسات test (CI).

## المرحلة 10 — التحقق النهائي
`pub get` → `build_runner` → `analyze` (صفر) → `test` (كامل أخضر) → `flutter clean && flutter build apk --release --dart-define-from-file=.env --target-platform android-arm64` → تحديث الحكم + ملف الذاكرة.

**الحجم:** ~55–65 ملفاً. **بواباتك المباشرة:** نشر schema (0.3) والتحقق من صف sessions بعد دخول تجريبي (2). **أكبر مخاطرة:** نقل كيانات courses (5a) — تخففها البوابات بعد كل خطوة.